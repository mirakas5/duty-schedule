# Offline install for closed-network Windows. Plain uvicorn (no [standard]).
# Keep this file ASCII-only: pip reads it with the system codepage (cp949 on Korean Windows).
fastapi==0.115.0
uvicorn==0.30.6
sqlalchemy==2.0.35
openpyxl==3.1.5
python-multipart==0.0.12
pydantic==2.12.5
